# Gemini media analysis

`gemini_analyze_video`, `gemini_analyze_image`, `gemini_generate_image`.

Install: `cp extras/gemini/gemini.py "$KBOTS_OVERLAY/tools/"`

Auth: vault key `secrets/gemini-api-key`.
