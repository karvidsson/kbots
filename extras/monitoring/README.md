# Monitoring

`rss_read`, `web_watch` (page-change detection), `weather`. No API keys.

Install: `cp extras/monitoring/monitoring.py "$KBOTS_OVERLAY/tools/"`

The **news** extra imports helpers from this module — install this one first.
