# GitHub

6 read/comment tools (`gh_*`): issues, PRs, comment, create_issue.

Install: `cp extras/github/github.py "$KBOTS_OVERLAY/tools/"`

Auth: vault key `secrets/github-token` (or `github-token` / `GITHUB_TOKEN`).
