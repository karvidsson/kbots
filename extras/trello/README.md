# Trello

8 tools (`trello_*`): boards, lists, cards, card detail, activity, create/archive/delete.

Install: `cp extras/trello/trello.py "$KBOTS_OVERLAY/tools/"`

Auth: vault key `secrets/trello-credentials.json` — JSON with `key` + `token`.
