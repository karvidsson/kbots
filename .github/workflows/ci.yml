name: CI

on:
  push:
    branches: [main]
  # `edited` matters for the title/body leak scan below: it reads the
  # description from the event payload, which is frozen at the triggering
  # event. Without this, fixing a leaked name in the description leaves the
  # check permanently red — re-running replays the same stale payload, and the
  # only way to clear it is an unrelated commit. A guard you cannot satisfy
  # after obeying it teaches people to ignore it.
  pull_request:
    types: [opened, synchronize, reopened, edited, ready_for_review]

permissions:
  contents: read

jobs:
  gates:
    name: lint + tests
    runs-on: ubuntu-latest
    # CI runners have no overlay, so the roster-name leak checks would
    # silently skip here — the one place that sees every merge. The
    # deployment's names live in a repo secret (never in the tree) and are
    # injected for the leak tests. Empty/unset degrades gracefully to
    # shape-checks only (e.g. on forks, which never see secrets).
    env:
      KBOTS_LEAK_NAMES: ${{ secrets.KBOTS_LEAK_NAMES }}
    steps:
      - uses: actions/checkout@v4
        with:
          # Full history: tests/test_no_install_leaks.py scans every commit
          # message, and a shallow clone would make that check pass vacuously.
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - run: uv sync --extra dev
      - run: uv run ruff check .
      - run: uv run pytest -q

  pr-metadata:
    name: no install leaks in PR title/body
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # A pull request description is the one surface no test can reach: it
      # lives on GitHub, not in the tree, and it is where live account IDs
      # actually leaked. Title and body arrive via env rather than ${{ }}
      # interpolation so that a description can never be run as shell.
      - name: Scan PR title and body
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_BODY: ${{ github.event.pull_request.body }}
          KBOTS_LEAK_NAMES: ${{ secrets.KBOTS_LEAK_NAMES }}
        run: |
          printf '%s\n%s\n' "$PR_TITLE" "$PR_BODY" > pr-metadata.txt
          python3 scripts/check_install_leaks.py --text pr-metadata.txt
